# PLAN · T-CAP-GENERAL-P0

```text
STATUS: BINDING · 本阶段主线核心
DATE: 2026-08-08
GOAL: 根因补强 Pico 通用能力 → 再测；禁止定向优化/捷径
MODE: SOLO · 仅 EXCELLENT 晋级 · CLAIM-WB: NO
BASE: 38b6d86de67122f7b7a77484e9d22124bd8a280b
PARENT: #333 WB-STYLE 工程 PASS · #329 E2E-DEFAULT
```

## 0. 业主铁律（本卡最高优先级）

```text
1) 测试暴露问题 → 修产品通用能力，不是修「那几道题」
2) 绝对禁止定向优化 / 走捷径：
   - 不为 W1–W5 / 固定题干 / 固定文件名硬编码特判
   - 不为绿卡降标准、装成功、只通演示路径
   - 不在 prompt 里写死考题答案结构当「能力」
3) 必须：定位根因 → 通用机制补强 → 用「未见过的开放题」回归
4) 绿是结果；产品变强是目的
```

## 1. 锁定句

```text
目标：Web 上 WorkBuddy 程度（六条）
方案：Pico 整车 + Pi + DeepSeek
执行：单窗 SOLO · 根因通用补强 · 再测
不做：Dify · 场景卷对标 · 双核 · 考题特判 · 自签 CLAIM
```

## 2. 测试暴露 → 根因假设（通用，非题面）

| 现象（#333 等） | 根因假设（产品层） | 通用能力目标 |
|-----------------|-------------------|--------------|
| 多交付任务常塌成 **单长文/单产物** | 编排/交件层未把「多产物」当默认一等公民；NL 分件意图弱 | **C1 多产物交付** |
| 流水线步骤只在聊天里 | 无「阶段资产必须落盘」的通用策略 | **C2 流水线资产落盘** |
| 可运行物仅书面自测 | 无「生成后自检可运行」通用环 | **C3 可运行物自检** |
| 改结论不联动多文件 | 同会话修订不强制版本化/分文件更新 | **C4 修订联动** |
| 脚本 timeout vs 人眼绿 | 流式结束/产物就绪信号弱；观测不足 | **C5 完成信号与观测** |
| 补测要靠「硬咒语」才齐 | 默认策略偏「能交差」而非「工程交付」 | **C1–C4 默认即硬** |

> 实现时：**允许**修正根因假设（有日志/代码证据），**禁止**用「只对 W5 生效的 if」当修复。

## 3. 能力切片（IN · 顺序）

| ID | 名称 | 定义成功（通用） |
|----|------|------------------|
| **G0** | 根因冻结 | 每条现象对应代码/配置位置 + 不采用方案清单（反捷径） |
| **G1** | 多产物默认 | 用户意图含 ≥2 交付物时，**默认**多 artifact；单文件多标题 = 失败策略 |
| **G2** | 流水线落盘 | 多阶段任务每阶段可独立文件；不依赖题面关键词表 |
| **G3** | 可运行自检 | HTML/前端类产物生成后有检查环（结构/关键交互或明确「未跑通」） |
| **G4** | 修订联动 | 「改结论」类指令更新相关已交付文件或出带版本号新文件 |
| **G5** | 完成与观测 | run 结束条件清晰；产物列表可机读；减少纯 timeout 误判 |
| **G6** | 开放回归 | **新题干**（非 W1–W5 原文）≥3 道验证 C1–C4；旧 W 抽检不退化 |
| **G7** | 证据与反捷径审计 | PR 说明「通用点」；禁止列表自检；CLAIM-WB:NO |

## 4. 反捷径清单（OUT · 发现即 FAIL）

```text
❌ if prompt contains "习惯打卡" / "灰度 10%" / "七猫" …
❌ 仅对 TEST-TASK 文件路径特判
❌ 把 W1–W5 全文塞进 system 当标准答案
❌ 测试环境专用开关让产物变多、生产仍单文件
❌ 降级 ACCEPT「单文件也算 PASS」
```

## 5. 执行序

```text
G0 根因冻结（可读代码+日志）
→ G1 多产物（先）
→ G2 流水线落盘
→ G3 可运行自检
→ G4 修订联动
→ G5 完成信号（可与 G1 同 PR 若耦合）
→ 合 main · 装 tip · health sha
→ G6 开放回归（新题）+ 可选旧 W 抽检
→ G7 证据 · CLAIM NO
```

仅 EXCELLENT 晋级。任一切片用捷径 = 整包打回。

## 6. 与旧卡关系

| 卡 | 关系 |
|----|------|
| #329 E2E-DEFAULT | 默认闲聊底盘；本卡不替代 |
| #333 WB-STYLE | 已工程 PASS；本卡**不**再为 W 题特修；G6 可用**新**开放题 |

```
════════════════════════════════════
BINDING · CAP-GENERAL-P0
根因 · 通用 · 再测 · 禁捷径
════════════════════════════════════
```
