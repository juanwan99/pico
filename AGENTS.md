# Pico agent rules (v0)

- Product: AI foundation only. No school SaaS rebuild.
- Prefer Kimi Agent + model **APIs**; no custom agent framework.
- Tenant fail-closed; no dual-run legacy AI.
- Small PRs; CANDIDATE + CI + independent review before merge when CI exists.
- Night: long tests OK; no unattended merge to default branch.
