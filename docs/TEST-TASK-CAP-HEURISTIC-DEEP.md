# TEST · T-CAP-HEURISTIC-DEEP

## 原则

```text
新题必须与 P1-A/B/C 同构但用词完全不同
禁止再用 义卖/广播稿/低成本两项/番茄钟 作为唯一绿路径
人类打开截图必填
```

### D6-1 隐式包装（新领域）

```text
请准备一套「开源社区双月会」物料：议程、贡献者指南、会议主持卡、会后纪要模板。
按完整会议需要自行拆成多份独立可下载文件，不必问我要几个文件。
```

### D6-2 软修订（新措辞）

```text
先做三阶段文件：现状摘要 → 选项对比 → 推荐决策。
（待产物出来后）下一轮只说：
「上次推荐太激进了，改成保守方案，只保留零成本动作。」
禁止依赖特定成语；须更新决策文件。
```

### D6-3 HTML（新应用）

```text
单页 HTML：简易「待办勾选」列表，本地打开可添加/勾选/删除。
浏览器打开必须见 UI。自检写明 L0/L1。
```

### D6-4 闲聊抽检

```text
你是什么模型
```

回执：人类打开 Y/N · 截图路径 · 可读可用 Y/N
