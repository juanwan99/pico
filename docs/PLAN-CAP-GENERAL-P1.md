# PLAN · T-CAP-GENERAL-P1

```text
STATUS: BINDING · 主线续 · 深度优化
DATE: 2026-08-08
PARENT: #335 T-CAP-GENERAL-P0 PASS @ fffb34d
GOAL: 隐式多交付 / 中性意图 / 完成可机读 / 可运行更真 · 仍禁捷径
MODE: SOLO · 仅 EXCELLENT · CLAIM-WB: NO
BASE: fffb34d7c1570007170aeb539e011e015cd21fe9
```

## 0. 铁律（继承并加重）

```text
根因 → 通用能力 → 新题再测
禁止：考题特判、W1–W5/N1–N4 关键词 if、测试后门、降 ACCEPT、假成功
绿是结果，不是目的
```

## 0.5 验收主权：人类视角（BINDING）

```text
验收标准以「人」为准，不以 LLM / 单测 / 机读字段为准。
HTML：人要打开能用的界面，不要一堆仅机器可读的代码交差。
文稿/表/PPT/图/包：人要下载打开能读、能分清、能转发使用。
机器 ok ≠ 人类 PASS。人类打不开/读不懂 = 产品失败。
```

细节见 ACCEPT §0。

## 1. 锁定句

```text
目标：Web 上 WorkBuddy 程度（六条）
方案：Pico 整车 + Pi + DeepSeek
执行：单窗 SOLO · P0 之上深度通用补强
不做：Dify · 场景卷 · 双核 · 定向刷题 · 自签 CLAIM
```

## 2. P0 余量 → 本卡根因

| P0 后仍弱 | 根因假设 | 本卡通用目标 |
|-----------|----------|--------------|
| 要用户把「3 个独立文件」说死才稳 | 意图检测偏**显式数量/分文件措辞**；缺「方案包/套件/一整套」隐式多交付 | **H1 隐式包装意图** |
| 检测词「倒计时」略贴题 | runnable 特征过窄/过贴场景 | **H2 中性化检测** |
| HTML 仅结构自检，真交互靠人 | 无可选 headless 冒烟或明确 not_run 分级 | **H3 可运行验证分层** |
| 脚本 timeout 与人眼绿打架 | 客户端未统一消费 `delivery.summary` / 终态 | **H4 完成信号消费** |
| 修订有时靠再念咒语 | 修订策略未覆盖「软改口」与产物图更新 | **H5 修订图更稳** |
| 默认 deepseek 直连仍可能绕过工程环 | 工程意图强制 agent 的边界仍可能漏 | **H1/H6 强制工程环补漏** |

## 3. 切片（IN）

| ID | 名称 | 成功定义（通用） |
|----|------|------------------|
| **H0** | 余量冻结 | 对照 P0 审查黄项 + 代码点；反捷径清单更新 |
| **H1** | 隐式多交付 | 「完整方案包/交付套件/一整套材料/从…到…全套」等**不报数字**也触发多产物计划与 min_artifacts≥2（可配置启发式，**无业务题名**） |
| **H2** | 检测中性化 | 去掉/弱化过贴场景词；单测用中性语料；回归 HTML **禁止**依赖「倒计时」触发 |
| **H3** | 可运行分层 | L0 结构自检（已有）+ L1 可选无头冒烟或明确 `verification_level`；失败/跳过必须诚实枚举 |
| **H4** | 完成信号 | 公网/脚本路径优先 `run.status` + `delivery.summary`；文档+最小脚本或探针；减少纯 sleep/timeout 判死 |
| **H5** | 修订图 | 软改口（「还是改成…」「优先级调一下」）也能联动已交付文件；通用，无固定业务句 |
| **H6** | 强制工程环补漏 | 工程意图下不得静默直连无工具交差；契约测 |
| **H7** | 开放回归 | **全新题干**≥4（禁复制 P0 N 题/W 题原文） |
| **H8** | 证据+反捷径 | PR · tip · X1–X4 · CLAIM NO |

## 4. 反捷径（一票否决）

```text
❌ if "图书漂流" / "会议倒计时" / "竞品周" / W1–W5
❌ 仅对 G6 旧题回归绿、新隐式题红却报 PASS
❌ 测试 env 加大 min_artifacts、生产不加
❌ 把 N1–N4 全文塞 system
```

## 5. 执行序

```text
H0 → H1+H2（可同 PR）→ H3 → H5 → H6 → H4
→ 合 main · 装 tip · health sha
→ H7 新题回归 → H8 结案
```

## 6. 与旧卡

| 卡 | 关系 |
|----|------|
| #335 P0 | 底座；本卡加深，不重做 P0 |
| #333 / #329 | 抽检不退化；非本卡主验收 |

```
════════════════════════════════════
BINDING · CAP-GENERAL-P1
隐式包装 · 中性检测 · 完成可机读 · 禁捷径
════════════════════════════════════
```
