# apps/

| Dir | Role |
|-----|------|
| `nextchat/` | **Only product UI** |
| ~~web/~~ | **Removed** — do not restore as default preview |

If a PR re-adds `apps/web` as the product shell, reject unless owner explicitly re-opens that decision.
