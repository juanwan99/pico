# Pico — runtime pins (see docs/HANDOFF-WB-PI.md)
# Python >= 3.12 recommended (legacy kimi-agent-sdk requires 3.12+)
# Product multi-step kernel = Pi (no kimi-agent-sdk required on main path)

# API
fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
pydantic>=2.9,<3
pydantic-settings>=2.6,<3
httpx>=0.27,<1
pyjwt[crypto]>=2.9,<3
python-multipart>=0.0.12
sse-starlette>=2.1,<4

# DB
sqlalchemy[asyncio]>=2.0,<3
aiosqlite>=0.20,<1

# Orchestrator — OpenAI-compatible models (DeepSeek primary)
openai>=1.50,<3
python-dotenv>=1.0,<2

# Sandbox S2 HTML→PNG raster (true image bytes; no host Chrome)
pillow>=10.4,<12

# Thin Office adapters (edit existing OOXML; not a self-built engine)
python-docx>=1.1,<2
python-pptx>=1.0,<2
openpyxl>=3.1,<4

# Legacy Kimi Agent path (optional rollback; requires Python >= 3.12)
# kimi-agent-sdk==0.0.5
# kimi-cli==1.12.0
