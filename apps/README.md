# apps/

| Dir | Role |
|-----|------|
| **`librechat/`** | **Only product UI shell (MIT)** — default |
| ~~nextchat/~~ | Removed — do not restore |
| ~~web/~~ | Removed — do not restore as default preview |
| ~~workbench/~~ | Removed — do not restore |

If a PR re-adds `apps/web` / `nextchat` / `workbench` as the product shell, reject unless owner explicitly re-opens that decision.  
Do **not** fork/decompile WorkBuddy; clean-room IA only.
