# ACCEPT · T-CAP-GENERAL-P0

```text
RULE: 仅 EXCELLENT 晋级 · 捷径一票否决
CLAIM-WB: NO
```

## 捷径否决（任一命中 = 切片 FAIL + 不得晋级）

| ID | 否决条件 |
|----|----------|
| X1 | 代码/配置出现针对已知考题关键词的特判分支 |
| X2 | 仅测试/演示环境多产物、生产路径仍单文件 |
| X3 | 用降低验收或假成功换绿 |
| X4 | PR 无法说明「对任意同类意图」如何生效 |

## G0 根因冻结

| EXCELLENT |
|-----------|
| 现象→文件/函数/策略 表完整 |
| 每条有「不做什么（反捷径）」 |
| 可被第三人复读 |

## G1 多产物默认

| EXCELLENT |
|-----------|
| 通用意图检测或计划阶段识别「多交付」 |
| ≥2 交付时强制/默认多 artifact；单文件多 H1 冒充 = fail-closed 或自动拆 |
| 单测/契约测**不**绑定 W5 原文 |
| 公网用**未见过**的多交付题验证 ≥1 次 |

## G2 流水线落盘

| EXCELLENT |
|-----------|
| 多阶段任务每阶段可落独立文件（机制通用） |
| 不依赖「小说/扫榜」等业务词才触发 |
| 开放题验证阶段文件 ≥3 |

## G3 可运行自检

| EXCELLENT |
|-----------|
| HTML/JS 类产物有自动检查或结构化自检报告 |
| 失败时诚实「未验证/失败点」而非「已可运行」空话 |
| 不写死 habit-checkin 文件名 |

## G4 修订联动

| EXCELLENT |
|-----------|
| 改关键结论后相关产物更新或出版本化新文件 |
| 通用「修订」策略，非「灰度 10%」字符串特判 |

## G5 完成与观测

| EXCELLENT |
|-----------|
| 产物列表可查询；终态与工具成功可区分 |
| 文档或代码说明如何避免「仅 timeout」误判 |

## G6 开放回归

| EXCELLENT |
|-----------|
| **≥3 道新题干**（禁止复制 TEST-TASK-WB 原文）覆盖多产物+流水线+修订 |
| 默认路径不改模型 |
| 旧能力抽检：默认闲聊仍通（#329 级最小） |
| 任一道靠捷径才过 = 整 G6 FAIL |

## G7 证据

| EXCELLENT |
|-----------|
| PRODUCT sha · PR 列表 · 反捷径自检表 |
| CLAIM-WB-DEGREE-WEB: NO |

## 包级

```text
CAP_GENERAL_P0: PASS ⇔ G0–G7 全 EXCELLENT 且无 X1–X4
```
