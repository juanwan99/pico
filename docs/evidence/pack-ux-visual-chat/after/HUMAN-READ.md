# T-UX-VISUAL-CHAT · 本地 after（合装前）

```text
mode: local-built-css (vite build --mode ci → index.DD9tyQiC.css)
公网 after: 黄卡放行装 tip 后再拍，hash 必须 ≠ before
CLAIM-WB: NO
```

对照 before 人话：

| 帧 | before 中间难看 | after 人眼 |
|----|-----------------|------------|
| V1 | 两个标题 + 两排胶囊仪表盘 + 紫线圈 | 只留「Pico，我帮你」；输入写「发消息」；无胶囊；一行底栏 |
| V2 | + 里工作空间/默认权限 | 只有 快速 / 深度 / 上传附件 |
| V3 | pico-fast 芯片、第二套 +、OpenAI 名片、空沙箱半屏 | 无顶栏芯片；两句对话；只有复制；右栏默认不开 |
| V4 | （公网未见来源/打开文件/登录） | 右栏只有沙箱网页，无来源、无打开文件、无登录 |
| V5 | 390 顶栏五芯片 | 390 中间标题+一行输入，无芯片条 |

左栏常驻：新对话 + 历史，其它进更多。

公网 before 五帧仍以 `../before/` 为准（真登录、tip `5a05741…`）。
本目录 V1–V5 是合装前本地对照，**不是**公网 after。
