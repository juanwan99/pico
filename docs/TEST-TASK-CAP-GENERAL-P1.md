# TEST · T-CAP-GENERAL-P1

## 原则

```text
必须新题干 · 至少 1 道「不说几个文件」的隐式包装
禁止复制 P0 N1–N4 / W1–W5 原文
验收主权：人类视角 —— 打开/下载后真人能用能读
禁止：仅源码墙、仅工具 ok、仅 LLM 自称已验证
HTML 必须浏览器打开见 UI；文稿必须打开见可读正文
```

每题回执必填：

```text
人类打开: Y/N
人类可读/可用: Y/N
证据: 截图路径或「诚实未打开」
```

## H0

Issue：`## H0 GAP-FREEZE`

## H7 开放题示例（执行可改写语义，须保持考点）

### P1-A 隐式多交付（核心）

```text
请做一套「校园义卖」筹备材料包：规则、摊位布局说明、志愿者须知、当日广播稿。
直接交付可下载成品。不要问我要几个文件——按完整筹备需要自行拆成多份独立文件。
```

### P1-B 流水线 + 软修订

```text
做「订阅转化」观察流水线：
阶段1 假设数据来源说明 → 阶段2 三条假设洞察 → 阶段3 本周行动。
每阶段独立文件。
做完后我说：算了，本周行动先只做低成本两项，别的顺延。
请更新阶段3（版本号或覆盖），1–2 可追溯。
```

### P1-C 中性可运行 HTML

```text
生成单页 HTML：简易「番茄钟」——开始/暂停/重置，本地 file 打开可用。
请做自检并写明验证级别（结构 only / 已交互 / 未跑交互）。
禁止空口「全部完美」。
```

### P1-D 默认闲聊

```text
不改模型。你是什么模型
```

### P1-E（可选）显式多文件对照

```text
请分别交付两个独立文件：A 名词解释十条；B 配套练习五题。禁止合并。
```

用于证明显式路径未回归；**不得**用 P1-E 单独代替 P1-A 给 H1 过关。

## 记录模板

```text
## H# SELF-ACCEPT
通用机制:
反捷径 X1-X5:
开放题:
等级:
CLAIM-WB: NO
```
